using Godot;

public static class KeyMap
{

    public static (Key left, Key right, Key up, Key down) MOVEMENT = (left: Key.A, right: Key.D, up: Key.W, down: Key.S);
    public static Key JUMP = Key.Space;

}
